#!/bin/sh
# Design Lab pre-push gate — blocks a push if the registry fails validation.
# Installed into .git/hooks/pre-push by scripts/install-hooks.ps1.

ROOT="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
node "$ROOT/scripts/check-registry.mjs"
exit $?
